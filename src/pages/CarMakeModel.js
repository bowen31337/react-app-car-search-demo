import React from 'react'
import CarMakeModel from '../containers/CarMakeModelContainer'

const CarMakeModelPage = props => {
	return (
		<div className="car-make-model-containter">
			<CarMakeModel {...props}/>
		</div>
		)
}

export default CarMakeModelPage
